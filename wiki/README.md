# wiki/ — the readings

LLM-authored, interlinked, **cited** markdown. One concept per page. Page types: `unitNN-<slug>`
(chapter reading), `concept-<slug>`, `theory-<slug>`, `study-<slug>`, `person-<slug>`.

Every page carries YAML frontmatter and a `## Sources` section. Browse in Obsidian; the graph view
shows hubs vs. orphans. See page conventions in [`../CLAUDE.md`](../CLAUDE.md).
